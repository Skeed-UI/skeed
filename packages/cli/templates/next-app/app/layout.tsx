import './globals.css';
import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: '{{APP_TITLE}}',
  description: 'Generated by Skeed.',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
{{SKEED_FONT_HEAD_LINKS}}
      <body>{children}</body>
    </html>
  );
}
